apiVersion: v1
kind: Service
metadata:
  name: messenger-api-svc
  namespace: production
  labels:
    app: messenger-api
spec:
  type: ClusterIP
  selector:
    app: messenger-api
  ports:
    - port: 80
      targetPort: 8080     # .NET 9 écoute sur 8080
      name: http
