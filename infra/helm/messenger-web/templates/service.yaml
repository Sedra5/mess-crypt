apiVersion: v1
kind: Service
metadata:
  name: messenger-web-svc
  namespace: production
  labels:
    app: messenger-web
spec:
  type: ClusterIP
  selector:
    app: messenger-web
  ports:
    - port: 80
      targetPort: 80       # NGINX écoute sur 80
      name: http
