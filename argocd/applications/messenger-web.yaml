# argocd/applications/messenger-web.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: messenger-web
  namespace: argocd
spec:
  project: production

  source:
    repoURL: https://github.com/Sedra5/mess-crypt
    targetRevision: main
    path: infra/helm/messenger-web
    helm:
      valueFiles:
      - values.yaml
      - values-prod.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: production

  syncPolicy:
    automated:
      prune: true # Supprimer les ressources supprimées du Git
      selfHeal: true # Resynchroniser si drift détecté
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m
        factor: 2
